{
  "name": "dr-science",
  "version": "1.0.0",
  "type": "module",
  "description": "Dr. Science Educational Platform - أستاذ وحيد حامد",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "vite": "^5.2.11",
    "gh-pages": "^6.1.1"
  }
}
